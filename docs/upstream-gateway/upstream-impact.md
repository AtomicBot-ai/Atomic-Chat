# Upstream impact

Made 2026-09-14T00:35:21.826Z for `upstream/main` at `7eb04761f` against `HEAD` at `9a3aac704`.
0 upstream commits since the last sync; 0 files would conflict.

Every flagged row needs a decision before the merge: **keep-ours**, **take-theirs** or **adapt**.
Record it in the tracker's Upstream impact sheet (or the JSON next to this file); `make upstream-gate` refuses until all are made.

Upstream touches none of Radium's features.

Untouched: Radium Media, Media in the sidebar, Atomic Code workspace stays removed, The product is called Radium, Data folder moves to Radium on first launch, Launch at startup survives the rename, Saved assistants say Radium, Radium never auto-updates, Windows installers carry verified engine backends, Chat, Cloud and API live in Settings, Windows window controls, Uncensored filter in the Hub filter menu, Unsigned Windows test installer from GitHub Actions.
