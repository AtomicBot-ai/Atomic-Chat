[package]
name = "tauri-plugin-llamacpp-upstream"
version = "0.6.599"
authors = ["Atomic Chat <service@atomic.chat>"]
description = "Tauri plugin for managing upstream ggml-org/llama.cpp server processes (vanilla, no TurboQuant)"
license = "MIT"
repository = "https://github.com/AtomicBot-ai"
edition = "2021"
rust-version = "1.77.2"
exclude = ["/examples", "/dist-js", "/guest-js", "/node_modules"]
links = "tauri-plugin-llamacpp-upstream"

[dependencies]
byteorder = "1.5.0"
jan-utils = { path = "../../utils" }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sysinfo = "0.34.2"
tauri = { version = "2.5.0", default-features = false, features = [] }
thiserror = "2.0.12"
tokio = { version = "1", features = ["full"] }
tauri-plugin-hardware = { path = "../tauri-plugin-hardware" }

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
reqwest = { version = "0.11", features = ["json", "blocking", "stream", "native-tls"] }

[target.'cfg(any(target_os = "android", target_os = "ios"))'.dependencies]
reqwest = { version = "0.11", features = ["json", "blocking", "stream", "rustls-tls"], default-features = false }

[dev-dependencies]
tempfile = "3.0"
filetime = "0.2"

[build-dependencies]
tauri-plugin = { version = "2.3.1", features = ["build"] }
